@{
    ViewData["Title"] = "NSNS Account";
}

@if (TempData["ErrorMessage"] != null)
{
   
    <div class="alert alert-danger">
        @TempData["ErrorMessage"]
    </div>
       
}

<iframe src="@ViewBag.HomePageUrl"
        title="Organization home page"
        width="100%"
        height="3400px"></iframe>
